import os
import io
import args
import test.conductor
import log
import ..setup

def parse(parser) {
  parser.add_command(
    'test', 
    'Run the tests'
  )
}

def run(value, options, success, error) {
  catch {
    conductor.conduct(setup.TEST_DIR)
  } as error {
    echo error.message
  }
}
