import os

def parse(parser) {
  parser.add_option(
    'version', 
    'Show Zuri version',
    {
      short_name: 'v'
    }
  )
}

def get(value) {
  echo 'Zuri v${os.version}'
  echo 'Running on ZuriVM v${os.vm_version}'
}
