## TODO

- Document `@to_index`
- Builtin PDF module (named `pdf`)
- Builtin JSON-RPC module (named `rpc`) 
- YAML module (named `yaml`)
- TOML module (named `toml`)
- PostgreSQL module (named `postgres`)
- MySQL and MariaDB module (named `maria`)